//Kieu du lieu enum
// 100 created taang len
//Khi chạy chương trình này kết quả sẽ thông báo
//lên trình duyệt số 0 => như vậy nếu ta không thiết lập giá trị cho chúng thì nó
//sẽ tính theo thứ tự và bắt đầu bằng 0.

// enum STATUS { CREATED, PROCCESS, FINISH};

// let todoStatus : STATUS =STATUS.CREATED;

// console.log(todoStatus);